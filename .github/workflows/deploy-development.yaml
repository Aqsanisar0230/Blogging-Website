name: Deploy to Development

on:
  push:
    branches:
      - develop

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install dependencies
        run: npm install

      - name: Build website
        run: npx parcel build index.html blog.html contact.html --public-url . --no-cache

      - name: Upload artifact
        uses: actions/upload-artifact@v4  #  Correct version
        with:
          name: build-artifact
          path: dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment: 
      name: github-pages
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download artifact
        uses: actions/download-artifact@v4  #  Correct version
        with:
          name: build-artifact
          path: dist

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v2  #  Correct version
